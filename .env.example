# Copy to .env and adjust. .env is gitignored.
BASE_URL=https://varsome.com
BROWSER=chrome
HEADLESS=true
PAGE_LOAD_TIMEOUT=45
EXPLICIT_WAIT=30
WINDOW_SIZE=1920,1080
# Credentials for the valid-login test (leave empty to skip it). Never commit real values.
VARSOME_USER=
VARSOME_PASSWORD=
